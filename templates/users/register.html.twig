{% extends "main.html.twig" %}

{% block content %}

    <div class="content">

        <div class="container">
            
            {{ form_start(form) }}
                {{ form_errors(form) }}
                {{ form_row(form.email, {'attr':{'class':'text-warning'}}) }}
                {{ form_row(form.password) }}
                {{ form_row(form.submit) }}
            {{ form_end(form) }}

        </div>

    </div>


{% endblock %}

